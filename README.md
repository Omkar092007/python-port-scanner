# Python Port Scanner
A simple, efficient multi-port scanner built in Python using the `socket` library to scan a range of target ports.

## Features
* Scans a specified target IP
* Multi-port scanning range (1-1024)
* Built using native Python sockets
